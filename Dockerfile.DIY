# ── Stage 1: 构建 ──────────────────────────────────────────
FROM node:22 AS builder
WORKDIR /app

COPY package.json package-lock.json* ./
COPY scripts/ ./scripts/

RUN npm ci

COPY . .
RUN npm run build


# ── Stage 2: 运行环境 ──────────────────────────────────────
FROM node:22-slim
USER root

RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential python3 python3-setuptools git \
    && rm -rf /var/lib/apt/lists/*

# 安装 Claude Code CLI
RUN npm install -g @anthropic-ai/claude-code && claude --version

# 从 builder 复制产物
COPY --from=builder /app/node_modules    /app/node_modules
COPY --from=builder /app/package.json   /app/package.json
COPY --from=builder /app/dist           /app/dist
COPY --from=builder /app/dist-server    /app/dist-server
COPY --from=builder /app/shared         /app/shared
COPY --from=builder /app/public         /app/public
COPY --from=builder /app/scripts        /app/scripts

WORKDIR /app

# 修复 node-pty 原生绑定
RUN node scripts/fix-node-pty.js

# ── 关键修复 ───────────────────────────────────────────────
# 1. 修复 node_modules/.bin 下所有 bin 的可执行权限
#    （COPY 保留了 root 构建时的权限，node 用户无执行权）
RUN chmod -R +x /app/node_modules/.bin/

# 2. 统一所有权，避免后续任何写入操作的 EPERM
RUN chown -R node:node /app

USER node

EXPOSE 3001

# node 直接调用，完全绕开 shell 和 PATH
CMD ["node", "/app/node_modules/.bin/cloudcli", "start", "--port", "3001"]
